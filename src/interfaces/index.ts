import { iAboutProps } from '@Interfaces/iAboutProps';
import { iCard } from '@Interfaces/iCard';
import { iHeaderProps } from '@Interfaces/iCommons';
import {
    iExperienceProps,
    IExperiencePlaceName,
    iExperienceDescription,
} from '@Interfaces/iExperienceProps';
import { iFooterProps } from '@Interfaces/iFooterProps';
import { iHeroProps } from '@Interfaces/iHeroProps';
import { iPortfolioProps } from '@Interfaces/iPortfolioProps';
import { iIcons } from '@Interfaces/iIcons';
import { iTechListProps } from '@Interfaces/iTechListProps';
import { iTexts } from '@Interfaces/iTexts';
import { iTheme } from '@Interfaces/iTheme';
import { iTimeDescriptionProps, iTimeList } from '@Interfaces/iTime';
import { iTechName } from '@Interfaces/iTechListProps';

export type {
    iAboutProps,
    iCard,
    iHeaderProps,
    iExperienceProps,
    IExperiencePlaceName,
    iExperienceDescription,
    iFooterProps,
    iHeroProps,
    iPortfolioProps,
    iIcons,
    iTechListProps,
    iTexts,
    iTheme,
    iTimeDescriptionProps,
    iTimeList,
    iTechName,
};
